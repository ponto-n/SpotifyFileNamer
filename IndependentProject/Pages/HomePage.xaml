<Page
    x:Class="IndependentProject.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IndependentProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />

        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="60"  HorizontalAlignment="Center" VerticalAlignment="Center"  Style="{StaticResource ApplicationTitleTextBlockStyle}"/>

        <Button Name="StartButton" FontSize="30" Content="Start" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Click="StartButton_Click" Style="{StaticResource ApplicationAccentedButtonStyle}"/>
        
    </Grid>
    
    
</Page>
